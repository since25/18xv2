# 获取视频播放进度

## 基本信息

- Path：`GET 域名 + /open/video/history`
- Method：`GET`
- 接口描述：获取视频播放进度。

## 请求参数

**Headers**

| 参数名称 | 参数值 | 是否必须 | 示例 |
| --- | --- | --- | --- |
| `Authorization` | `Bearer access_token` | 是 | `Bearer abcdefghijklmnopqrstuvwxyz` |

**Body(form-data)**

| 参数名称 | 参数类型 | 是否必须 | 示例 | 备注 |
| --- | --- | --- | --- | --- |
| `pick_code` | string | 是 | `b53gu6z3hvqji8wrm` | 文件提取码 |

## 注意事项

- 视频是否播放完毕可在文件列表的 `played_end` 字段中查看，`1` 表示播放完毕。

## 返回数据

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `state` | bool | 操作结果状态值，`true` 成功，`false` 失败 |
| `message` | string | 操作返回消息，成功时为空 |
| `code` | int | 操作返回号码，成功时返回 `0` |
| `data` | array | 数据 |
| `data.add_time` | int | 记录添加时间 |
| `data.file_id` | string | 文件 ID |
| `data.file_name` | string | 文件名称 |
| `data.hash` | string | 文件哈希值 |
| `data.pick_code` | string | 文件提取码 |
| `data.time` | string | 记录的已播放时长 |

> 更新：2025-06-06 17:54:19
> 原文：[语雀链接](https://www.yuque.com/115yun/open/gssqdrsq6vfqigag)
